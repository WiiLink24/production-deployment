# coredns
The `address.d` and `server.d` folders contain configuration files for coredns, that are mounted to its configuration folder.

### Note about `address` vs `server`
Server is used for name servers.
Address is used to force an IP address to a specific domain.