# dnsmasq
The `conf.d` folder contains configuration files for dnsmasq, that are copied to its configuration folder when the container is built.

### Note about `address` vs `server`
Server is used for name servers.
Address is used to force an IP address to a specific domain.